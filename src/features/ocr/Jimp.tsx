import React from "react";

type Props = {
	capImage: string
	setOcrImage: React.Dispatch<React.SetStateAction<string>>
}

const Jimp:React.FC<Props> = (props) => {
	

	return(
		<>
		</>
	)
}

export default Jimp;