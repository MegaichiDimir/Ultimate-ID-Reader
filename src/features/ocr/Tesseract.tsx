import React from "react";

type Props = {
	ocrImage: string
	setRoomId: React.Dispatch<React.SetStateAction<string>>
}

const Tesseract:React.FC<Props> = (props) => {

	return(
		<>
		</>
	)
}

export default Tesseract;